mod cli;
mod parser;
mod configuration;
//mod navigation;
//mod pod;

use crate::cli::Cli;
use clap::Parser;

fn main() {

/*
    match cli::run(Cli::parse()) {
        Ok(ok)=> ok,
        Err(e)=>  eprintln!("{}",e),
    }
*/
}



